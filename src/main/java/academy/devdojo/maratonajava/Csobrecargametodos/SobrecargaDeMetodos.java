package academy.devdojo.maratonajava.Csobrecargametodos;

public class SobrecargaDeMetodos {
    //A sobrecarga de métodos em Java é quando uma classe tem vários métodos com o mesmo nome, mas com assinaturas diferentes (diferente número ou tipo de parâmetros).
    // Isso permite usar o mesmo nome para realizar ações similares, mas com entradas diferentes.
}
